<h1>Dear <%= @order.name %></h1>

<p>Thank you for your recent order from The Pragmatic Store.</p>

<p>You ordered the following items;</p>

<table>
  <thead>
    <tr>
      <td>Quantity</td>
      <td>Title</td>
      <td>Amount</td>
    </tr>
  </thead>
  <tbody>
    <% @order.line_items.each do |line_item| %>
    <tr>
      <td><%= line_item.quantity %>&times;</td>
      <td><%= line_item.product.title %></td>
      <td class="item_price"><%= number_to_currency(line_item.total_price, unit: '¥') %></td>
    </tr>
    <% end %>
    <tr class="total_line">
      <td colspan="2">Total</td>
      <td class="total_cell"><%= number_to_currency(@order.total_price, unit: '¥') %></td>
    </tr>
  </tbody>
</table>
<p>We'll send you a separate e-mail when your order ships.</p>
